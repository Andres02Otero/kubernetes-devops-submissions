apiVersion: v1

kind: PersistentVolumeClaim

metadata:
  name: todo-app-image-pvc

spec:
  storageClassName: todo-app-image-storage

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 100Mi
